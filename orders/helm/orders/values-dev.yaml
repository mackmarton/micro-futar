replicaCount: 1

image:
  repository: orders
  pullPolicy: Always
  tag: "latest"

service:
  type: ClusterIP
  port: 8080
  targetPort: 8080

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 250m
    memory: 256Mi

autoscaling:
  enabled: false

config:
  serverPort: 8080
  springProfile: dev
  datasource:
    url: jdbc:postgresql://postgres-dev:5432/orders_db_dev
    username: orders_db_dev
    passwordSecretName: orders-db-secret
    passwordSecretKey: password
  kafka:
    bootstrapServers: kafka-dev:9092
  keycloak:
    url: http://keycloak-dev:8080

