apiVersion: v1
kind: Service
metadata:
  name: {{ include "logistics.fullname" . }}
  labels:
    {{- include "logistics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}
    protocol: TCP
    name: http
  selector:
    {{- include "logistics.selectorLabels" . | nindent 4 }}

